<?php

return [

    /*
    |--------------------------------------------------------------------------
    | اطلاع‌رسانی رویدادهای سایت در پیام‌رسان بله
    |--------------------------------------------------------------------------
    |
    | هر رویداد مهم سایت (ثبت سفارش، تغییر وضعیت، ورود و ثبت‌نام، نظر، کد
    | تخفیف) به یک چت بله فرستاده می‌شود تا مدیر بدون باز کردن پنل در جریان
    | باشد. ساخت ربات با @BotFather در بله انجام می‌شود و شناسه‌ی چت را
    | می‌توان با «php artisan bale:chats» بعد از فرستادن یک پیام به ربات دید.
    |
    | مثل بقیه‌ی سرویس‌های این پروژه، مقادیر از config خوانده می‌شوند نه
    | env()؛ چون با «php artisan config:cache» روی سرور، env() بیرون از
    | فایل‌های config همیشه null برمی‌گرداند و اطلاع‌رسانی بی‌سروصدا قطع می‌شد.
    |
    */

    'enabled' => (bool) env('BALE_ENABLED', true),

    /** توکن رباتی که BotFather بله داده است. */
    'token' => env('BALE_BOT_TOKEN'),

    /**
     * مقصد پیام‌ها؛ شناسه‌ی عددی چت خصوصی یا گروه.
     * برای چند مقصد، با کاما جدا کنید: BALE_CHAT_ID=123,-456
     */
    'chat_id' => env('BALE_CHAT_ID'),

    'api_url' => env('BALE_API_URL', 'https://tapi.bale.ai'),

    /** ثانیه؛ درگاه بله کند باشد نباید صفحه‌ی کاربر را نگه دارد. */
    'timeout' => (int) env('BALE_TIMEOUT', 8),

    /**
     * ارسال بعد از تحویل پاسخ به کاربر (terminating). با این کار تاخیر
     * شبکه‌ی بله به زمان پاسخ صفحه اضافه نمی‌شود. فقط برای دیباگ خاموشش کنید.
     */
    'defer' => (bool) env('BALE_DEFER', true),

    /*
    | کدام رویدادها ارسال شوند.
    |
    |   BALE_EVENTS=*                              همه (پیش‌فرض)
    |   BALE_EVENTS=order_placed,awaiting_call     فقط همین‌ها
    |   BALE_EVENTS_EXCEPT=customer_login          همه به‌جز این‌ها
    |
    | فهرست کلیدهای معتبر در App\Services\BaleNotifier::EVENTS است.
    */
    'events' => env('BALE_EVENTS', '*'),
    'events_except' => env('BALE_EVENTS_EXCEPT', ''),

    /*
    | ثانیه؛ یک خطای تکراری (همان فایل و همان خط) در این بازه فقط یک‌بار به
    | بله می‌رود. بدون این سقف، یک خطای پرتکرار در چند دقیقه ربات را پر می‌کرد.
    */
    'error_throttle' => (int) env('BALE_ERROR_THROTTLE', 300),

    /*
    | ثانیه؛ یک عبارتِ جستجو در این بازه فقط یک بار گزارش می‌شود. جستجوی
    | پرتکرار (یا کاربری که چند بار همان عبارت را می‌زند) نباید ربات را پر
    | کند؛ ارزشِ این رویداد در دیدنِ تنوعِ عبارت‌هاست نه تعدادشان.
    */
    'search_throttle' => (int) env('BALE_SEARCH_THROTTLE', 900),

    /*
    | سقف طول هر پیام. بله پیام خیلی بلند را رد می‌کند و آن‌وقت مدیر به‌جای
    | پیام ناقص، هیچ خبری نمی‌گیرد؛ پس متن بلند بریده می‌شود نه رها.
    */
    'max_length' => (int) env('BALE_MAX_LENGTH', 3500),

];
