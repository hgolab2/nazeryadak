<?php

return [

    /*
    |--------------------------------------------------------------------------
    | درگاه زرین‌پال
    |--------------------------------------------------------------------------
    |
    | این مقادیر قبلاً مستقیم با env() در کنترلر خوانده می‌شدند. اگر روی سرور
    | «php artisan config:cache» اجرا شود، env() بیرون از فایل‌های config
    | همیشه null برمی‌گرداند و درگاه پرداخت بی‌سروصدا از کار می‌افتد.
    |
    */

    'zarinpal' => [
        'merchant_id'  => env('ZARINPAL_MERCHANT_ID'),
        'callback_url' => env('ZARINPAL_CALLBACK_URL'),
        'sandbox'      => (bool) env('ZARINPAL_SANDBOX', false),

        /*
        | واحد پول ارسالی به زرین‌پال:
        |   IRT = تومان، IRR = ریال
        |
        | قیمت‌های سایت با تومان ذخیره و نمایش داده می‌شوند، پس IRT درست است.
        | اگر این مقدار فرستاده نشود، زرین‌پال مبلغ را ریال فرض می‌کند و
        | یک‌دهم مبلغ از مشتری کسر می‌شود.
        */
        'currency' => env('ZARINPAL_CURRENCY', 'IRT'),
    ],

    /*
    | شماره‌ای که با ثبت هر سفارش جدید پیامک اطلاع دریافت می‌کند.
    | خالی بگذارید تا پیامکی فرستاده نشود.
    */
    'notify_mobile' => env('ORDER_NOTIFY_MOBILE'),

];
