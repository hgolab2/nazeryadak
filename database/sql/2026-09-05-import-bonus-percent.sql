-- ============================================================================
-- تغییرات دیتابیس — درصد پاداش تصادفی ایمپورت
--
-- معادلِ مستقیمِ مایگریشن:
--   2026_09_05_000000_add_import_bonus_percent_to_products
--
-- روی سرور می‌توانید به‌جای این فایل فقط «php artisan migrate» بزنید؛ این فایل
-- برای وقتی است که دسترسی به artisan ندارید و مستقیم در phpMyAdmin کار می‌کنید.
--
-- بدون این ستون، ایمپورت موجودی با این خطا می‌افتد:
--   SQLSTATE[42S22]: Unknown column 'import_bonus_percent' in 'SET'
--
-- قبل از اجرا از دیتابیس بکاپ بگیرید.
-- ============================================================================


-- ---------------------------------------------------------------------------
-- ستون درصد پاداش
--
-- ایمپورت روی قیمت اکسل ۲۰٪ قطعی می‌گذارد، بعد بین ۰ تا ۵ درصد به‌صورت تصادفی
-- به همان مبلغ اضافه می‌کند و دقیقا همان مبلغ را به‌عنوان تخفیف کم می‌کند؛ پس
-- قیمت پرداختی همان ۲۰٪ می‌ماند و فقط روی کارت محصول تخفیف دیده می‌شود.
--
-- این ستون درصدِ به‌کاررفته را نگه می‌دارد تا قیمت عمده — که از روی قیمت پیش از
-- تخفیف بازسازی می‌شود — بتواند آن را از مبنا بردارد. بدون نگه‌داشتنِ عدد،
-- قیمت عمده به اندازه‌ی همان پاداشِ تصادفی بالا می‌رفت.
--
-- بازه‌ی مقدار ۰ تا ۵ است، پس TINYINT کافی است.
-- ---------------------------------------------------------------------------

ALTER TABLE `products`
    ADD COLUMN `import_bonus_percent` TINYINT UNSIGNED NOT NULL DEFAULT 0 AFTER `compare_at_price`;


-- ---------------------------------------------------------------------------
-- برگشت (rollback)
-- ---------------------------------------------------------------------------
-- ALTER TABLE `products` DROP COLUMN `import_bonus_percent`;
